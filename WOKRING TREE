WORKING ON--
    the work is done we just have to implement it on the 
    "Dev lets work" website and see if it works fluently 
    and then we set ti up on the web site.

    later on -- it needs some clean ups. (there are things we arent 
    using in side the work that need to be deleted.)